{
  "name": "moodlia-rubrics",
  "version": "1.2.1",
  "private": true,
  "description": "MoodlIA browser extension for importing Moodle rubrics from CSV and Excel files.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "npm run validate && npm test",
    "validate": "node scripts/validate-extension.mjs",
    "build:site": "node scripts/build-site.mjs",
    "build:extension": "npm run validate && powershell -NoProfile -ExecutionPolicy Bypass -File scripts/package-extension.ps1",
    "build": "npm run validate && npm run build:site && npm run build:extension",
    "dist": "npm run build",
    "package": "npm run build"
  },
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "@xmldom/xmldom": "^0.9.12",
    "fflate": "^0.8.3",
    "jsdom": "^30.1.1"
  }
}
